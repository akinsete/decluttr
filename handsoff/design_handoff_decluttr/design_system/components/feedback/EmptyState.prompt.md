Icon + title + description (+ optional action) block for any empty area — never leave blank space unexplained.

```jsx
<EmptyState
  icon={<TrashIcon />}
  title="Nothing in the trash yet."
  description="Deleted contacts land here for 30 days."
/>
```
