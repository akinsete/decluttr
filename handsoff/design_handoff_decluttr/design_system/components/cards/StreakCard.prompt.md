Compact single-row card surfacing the user's current streak; opens the streak-detail bottom sheet on tap.

```jsx
<StreakCard streakDays={4} supportingText="Longest: 9 days" onClick={openStreakDetail} />
```
